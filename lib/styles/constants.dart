import 'package:flutter/material.dart';

const aBackgroundColor = Colors.blueAccent;
const aPrimaryColor = Colors.blueAccent;
const aSecondaryColor = Colors.blue;
const aTextColor = Colors.white;

const aDefaultPadding = 20.0;

const aDefaultShadow = BoxShadow(
  offset: Offset(0, 12),
  blurRadius: 27,
  color: Colors.black12
);